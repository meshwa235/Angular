// import { TestBed } from '@angular/core/testing';
import { AppComponent } from './app.component';

describe('changeState', () => {
  it('change state to true'),() =>{
    const result = new AppComponent;
    expect(result).toBeTrue();
  }
});
